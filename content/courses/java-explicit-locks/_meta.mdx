---
title: "Explicit Locks"
slug: "java-explicit-locks"
description: "Why ReentrantLock exists when we have synchronized, tryLock with timeout, lockInterruptibly, fair vs unfair queuing, Condition as a superior wait/notify, ReadWriteLock's read/write separation, writer starvation, and StampedLock's optimistic read — the fastest read-mostly data structure primitive."
date: "2026-04-19"
tags: ["java", "reentrantlock", "readwritelock", "stampedlock", "concurrency", "explicit-locks"]
level: "advanced"
totalLessons: 8
category: "Java Concurrency"
icon: "threads"
accent: "#22d3ee"
draft: false
order: 4
prerequisites: ["java-synchronization-primitives"]
next: "java-atomic-lockfree"
---

Why ReentrantLock exists when we have synchronized, tryLock with timeout, lockInterruptibly, fair vs unfair queuing, Condition as a superior wait/notify, ReadWriteLock's read/write separation, writer starvation, and StampedLock's optimistic read — the fastest read-mostly data structure primitive.
