---
title: "Synchronization Primitives"
slug: "java-synchronization-primitives"
description: "The monitor model from first principles, synchronized method vs block vs static, intrinsic lock inflation (biased → thin → fat), volatile's visibility guarantee and the atomicity trap, Object.wait()/notify()/notifyAll() semantics, spurious wakeups, and deadlock via synchronized with lock-ordering as the fix."
date: "2026-04-19"
tags: ["java", "synchronized", "volatile", "monitor", "wait-notify", "concurrency", "deadlock"]
level: "intermediate"
totalLessons: 8
category: "Java Concurrency"
icon: "threads"
accent: "#06b6d4"
draft: false
order: 3
prerequisites: ["java-memory-model-concurrency"]
next: "java-explicit-locks"
---

The monitor model from first principles, synchronized method vs block vs static, intrinsic lock inflation (biased → thin → fat), volatile's visibility guarantee and the atomicity trap, Object.wait()/notify()/notifyAll() semantics, spurious wakeups, and deadlock via synchronized with lock-ordering as the fix.
