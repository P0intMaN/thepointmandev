---
title: "Clean Architecture"
slug: "clean-architecture"
description: "Why wrong dependencies make code hard to change, layered architecture (Presentation → Application → Domain → Infrastructure), the Dependency Rule, ports and adapters (hexagonal), the domain model (entities, value objects, aggregates), application services, what gets mocked in tests, and applying it all to a Spring Boot app."
date: "2026-04-19"
tags: ["java", "clean-architecture", "hexagonal", "ddd", "domain-model", "design", "spring"]
level: "intermediate"
totalLessons: 8
category: "Design Patterns"
icon: "network"
accent: "#3730a3"
draft: false
order: 5
prerequisites: ["behavioral-patterns"]
---

Why wrong dependencies make code hard to change, layered architecture (Presentation → Application → Domain → Infrastructure), the Dependency Rule, ports and adapters (hexagonal), the domain model (entities, value objects, aggregates), application services, what gets mocked in tests, and applying it all to a Spring Boot app.
